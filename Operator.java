public class Operator { // add symbol, subtract symbol, multiply symbol, divide symbol, exponent symbol,and much more
    private String symbol;

    public Operator(String symbol) {
        this.symbol = symbol;
    }
}
